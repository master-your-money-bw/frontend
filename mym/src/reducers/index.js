// import actions

const initializeState = {
    onboarding: false
}

export default function(state = initializeState, action ) {
    switch (action.type) {
        default: 
            return state;
    }
}